pipeline{
    agent any
    tools {
        maven 'Maven'   // ← MUST match the name in Jenkins UI
    }

    environment{
        MY_SECRET = credentials('demo-secret')
    }

    stages{
        stage('Build & Test'){
            steps{
                sh 'mvn clean verify'
            }
        }
    }
}